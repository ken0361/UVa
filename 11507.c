#include<stdio.h>
int main()
{
    int num;

    while(scanf("%d", &num)!=EOF&&num!=0)
    {
        for(int i=0; i<num; i++)
        {
            scanf("%s", )
        }
    }
    return 0;
}
