#include<stdio.h>
#include<string.h>
int main()
{
    int round;
    char ans[100], guess[100];

    while(scanf("%d%s%s", &round, ans, guess)&&round!=-1)
    {
        for(int i=0; i<strlen())
    }

    return 0;
}
